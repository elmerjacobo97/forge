{
  "name": "forge-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @forge/web dev",
    "build": "pnpm --filter @forge/web build",
    "preview": "pnpm --filter @forge/web preview",
    "test": "pnpm --filter @forge/web test",
    "doctor": "pnpm --filter @forge/web doctor"
  }
}
