# Forge

Project instructions are maintained in `AGENTS.md`.

Forge is a web-first pnpm workspace. The deployable app is `apps/web`; do not add Tauri, Rust, native IPC, or desktop-only dependencies unless a future desktop application has a real consumer.
