# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Full project conventions live in `AGENTS.md` — read it, it is authoritative. This file adds commands and architecture notes for working productively in the codebase.

## Commands

Run from the repo root (pnpm workspace, one app: `@forge/web` in `apps/web`).

- `pnpm install` — install all workspace dependencies
- `pnpm dev` — start the web app (Vite dev server)
- `pnpm build` — typecheck (`tsc`) then build `@forge/web`
- `pnpm test` — run the web test suite (Vitest) once
- `pnpm doctor` — run React Doctor against the web app

Single test file: `pnpm --filter @forge/web exec vitest run <path>` (or `pnpm --filter @forge/web exec vitest <path>` to watch). Vitest runs with `environment: "node"` and `globals: true` (see `apps/web/vite.config.ts`) — no DOM by default; tests are largely pure-function unit tests colocated as `*.test.ts` next to the code they cover (e.g. `apps/web/src/features/<feature>/utils/*.test.ts`).

There is no lint script configured in either `package.json`; do not assume ESLint is present.

## Architecture

**Workspace**: pnpm workspace rooted here, packages under `apps/*` and `packages/*`. Only `apps/web` exists today. Do not create empty packages speculatively, and do not add Tauri/Rust/native-IPC/desktop-only dependencies — this is a browser-only app (see project CLAUDE.md at repo root / `AGENTS.md`).

**Feature-first structure**: `apps/web/src/features/<feature>/` owns everything for that feature — `components/`, `hooks/`, `services/`, `schemas/`, `types/`, `utils/`. There are ~25 features, mostly standalone dev-utility tools (JSON formatter, JWT decoder, hash generator, etc.) plus larger stateful features (`dev-board`, `bookmarks`, `snippets`, `auth`, `settings`). Keep feature logic inside its feature folder unless it's genuinely shared.

**Routing**: TanStack Router, file-based, under `apps/web/src/routes/`. `_auth/` holds unauthenticated routes (login, register); `_authenticated/` holds everything behind auth, one thin route file per tool that composes the corresponding feature component. `routeTree.gen.ts` is generated by the `tanstackRouter` Vite plugin — never edit it by hand, it regenerates on `pnpm dev`/`pnpm build`.

**Tool registry**: `apps/web/src/lib/tools.ts` is the single source of truth for the tool catalog (id, route path, name, description, icon, category) that drives the home page and navigation. Adding a new dev-utility tool means: add a feature folder, add a route in `_authenticated/`, and register it in `tools.ts`.

**Appwrite data layer — two patterns coexist**:
- Dedicated collections for features with their own shape/scale needs (e.g. `bookmarks`, `dev-board` tickets/events/time-entries) — each has its own `VITE_APPWRITE_*_COLLECTION_ID`/`TABLE_ID` in `.env` and its own service module (`services/<feature>-service.ts`) calling `tablesDB` directly.
- A generic shared collection (`apps/web/src/lib/appwrite-data.ts`, backed by `VITE_APPWRITE_DATA_COLLECTION_ID`) for features that don't need a dedicated collection. Rows are shaped `{ userId, feature, payload }` with `payload` JSON-serialized; `fetchFeatureRows`/`createFeatureRow`/`deleteFeatureRow` are generic over the payload type. Prefer this for new features unless there's a real scaling/query reason for a dedicated collection.

Feature services generally support both a `localStorage`-backed offline/no-auth mode and an Appwrite-backed mode, switching on `isAppwriteEnabled(userId)` (config present + user logged in) — see `bookmarks-service.ts` for the canonical shape.

`apps/web/src/lib/appwrite.ts` constructs the shared `client`, `account`, and `tablesDB` singletons from `VITE_APPWRITE_ENDPOINT`/`VITE_APPWRITE_PROJECT_ID`; import these rather than constructing new Appwrite clients.

**Auth**: `features/auth/services/auth-service.ts` wraps Appwrite `Account` (register/login/logout) as a singleton class instance. `features/auth/hooks/mutations.ts` wraps it in TanStack Query mutations with toast feedback (`sonner`) and navigation side effects (redirect to `/dev-board` on login, `/login` on logout). Session state is cached under the `["session"]` query key.

**Aliasing**: `@/*` → `apps/web/src/*` (configured in both `vite.config.ts` and `tsconfig.json`).

**Styling**: Tailwind v4, theme tokens in `apps/web/src/index.css` (no separate `tailwind.config`). shadcn components in `apps/web/src/components/ui`, config in `apps/web/components.json`.

**Env**: Appwrite config lives in `apps/web/.env` (gitignored); `apps/web/.env.example` documents every `VITE_*` key required, including which collection IDs are dedicated vs. the shared generic-data collection. Only public `VITE_*` values belong there — no secrets.
