# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Forge — Tauri v2 desktop app bundling local-first developer utilities (JSON, UUID, JWT, Base64, hashes, QR, regex, timestamps, diff, HTTP tester, color, git commit helper, etc.). Product intent in `docs/product.md` (Spanish): each utility is an independent, offline, privacy-respecting module; principles are simple/fast/private/local.

## Stack

- Frontend: React 19 + TypeScript + Vite 7, Tailwind v4, shadcn (style `radix-nova`, baseColor `neutral`, icons `lucide`, CSS variables). Also: react-hook-form + zod, cmdk, sonner (toasts), next-themes, react-resizable-panels, @tanstack/react-router (file-based), @tanstack/react-query (infra for server-state/sync), @dnd-kit (drag-and-drop).
- Backend: Rust via Tauri v2; crate name `forge_lib`.
- Package manager: **pnpm** (lockfile + `pnpm-workspace.yaml` present; `tauri.conf.json` hardcodes `pnpm dev` / `pnpm build`).

## Commands

- `pnpm install` — install deps.
- `pnpm tauri dev` — run the full app (Rust shell + Vite frontend). This is the real dev command.
- `pnpm dev` — web-only Vite; `invoke()` calls fail (no Tauri runtime). Fine for pure UI work.
- `pnpm tauri build` — production bundle (runs `pnpm build`, then Rust bundler).
- `pnpm build` — `tsc && vite build` (frontend typecheck + build).
- `pnpm doctor` — runs `react-doctor` (a11y/perf/bundle audit). Not a build/test step.

## Verification

- **No `lint`, `test`, or formatter scripts exist.** `.prettierrc` is empty; no test runner is configured.
- Typecheck = `pnpm build` (runs `tsc`) or `pnpm exec tsc --noEmit`.
- `tsconfig.json` is strict: `noUnusedLocals`, `noUnusedParameters`, `noFallthroughCasesInSwitch` are all on — unused imports/vars fail the build.

## Architecture

- Two source trees: `src/` (frontend TS) and `src-tauri/` (Rust).
- Frontend entry: `src/main.tsx` -> `RouterProvider` (`src/router.ts`) -> `__root.tsx` layout (`src/routes/__root.tsx`). Root layout mounts `Sidebar` + `Header` + `<Outlet/>` (the active route). `App.tsx` and `app-shell.tsx` were removed when routing was added; do not reintroduce them.
- Routing: **TanStack Router file-based** with browser history. Routes live in `src/routes/**`. The Vite plugin `@tanstack/router-plugin` autogenerates `src/routeTree.gen.ts` — **do not hand-edit**, it is regenerated by `pnpm dev`/`pnpm build`. Adding a route = new file in `src/routes/`, the plugin picks it up next run.
  - `__root.tsx` — `createRootRouteWithContext<{ auth, queryClient }>` (root layout).
  - `index.tsx` — `/` redirects to `/dev-board`.
  - `_authenticated.tsx` — pathless layout route, `beforeLoad` auth guard, throws `redirect({ to: '/login' })` when `context.auth.isAuthenticated` is false. All tool routes are children of this.
  - `login.tsx` — public login page (placeholder UI; `auth.login` is a no-op stub until backend exists).
  - `_authenticated/<tool>.tsx` — one route per feature, mounts the feature component. Use `createFileRoute('/_authenticated/<id>')`.
- Auth: `src/lib/auth.ts` exports `AuthState` + `authState` stub. MVP has `isAuthenticated: true`. Router context injects it into every `beforeLoad`. Future backend wires OAuth PKCE here — the interface is the contract guards consume, so no routing code changes later.
- Deep linking: `tauri-plugin-deep-link` registers scheme `forge://` (config in `tauri.conf.json` `plugins.deep-link.desktop.schemes`). `src/lib/deep-link.ts` parses `forge://dev-board` → `router.navigate({ to: '/dev-board' })`, handles cold start (`getCurrent()`) + running (`onOpenUrl()`).
- Path alias `@/*` -> `src/*` (set in both `tsconfig.json` and `vite.config.ts`).
- Each utility lives in its own folder `src/features/<name>/` (currently 27 tools registered). New feature = new folder + new route file in `src/routes/_authenticated/<id>.tsx` + registration in `src/lib/tools.ts` (the `tools[]` array powers both the sidebar and the tray menu). Most utilities are purely client-side; only those needing the OS go through Tauri.
- `src/lib/tools.ts` — `ToolDef` carries `path` (equals `id`, equals `/<id>`) and `category` (groups tools in the sidebar and in the tray submenus — e.g. `Data`, `Generators`, `Encoders`, `Converters`, `Decoders`, `Testers`, `Validators`, `Text`, `Network`, `Git`, `Resources`, `Media`, `Productivity`). `getToolByPath()` is used by `__root.tsx` to resolve the active tool from the router location.
- System tray: `src-tauri/src/tray.rs` builds the tray icon (`build_tray`) with a static Show/Quit menu, then the frontend calls the `update_tray_menu` command on startup (and whenever `tools.ts` changes) to rebuild it into a per-category submenu (`rebuild_menu`) — this keeps the tray in sync with `tools.ts` instead of hardcoding it in Rust. Clicking a tool menu item emits a `navigate` event the frontend router listens for. Closing the main window hides it instead of quitting (`CloseRequested` handler in `lib.rs` calls `prevent_close()` + `window.hide()`); `src-tauri/src/window.rs::show_and_focus_main` is the shared show/focus helper used by both the tray and `RunEvent::Reopen` (macOS dock re-click).
- Rust entry: `src-tauri/src/main.rs` -> `forge_lib::run()` in `src-tauri/src/lib.rs`. The `_lib` suffix is intentional (avoids Windows crate-name collision) — keep it.
- Tauri commands live in `src-tauri/src/commands/` (module `commands`, declared in `lib.rs` via `mod commands;`), not inline in `lib.rs`. `lib.rs` only wires plugins + the `generate_handler!` list. Current commands: `hash_file::hash_file`, `read_file::read_file_bytes`, `tray_menu::update_tray_menu`, `git_status::git_status`, `git_diff::git_diff`, `git_commit::git_commit` (all platforms), `color::pick_color` (**macOS-only** — guarded by `#[cfg(target_os = "macos")]`).
- Adding a Tauri command: define `#[tauri::command] fn` in `src-tauri/src/commands/<name>.rs`, export it from `commands/mod.rs`, register it in `tauri::generate_handler![...]` in `lib.rs`, call from TS via `invoke("name", { args })` from `@tauri-apps/api/core`.
- Plugins registered in `lib.rs`: `tauri_plugin_opener`, `tauri_plugin_http`, `tauri_plugin_dialog`, `tauri_plugin_deep_link`, `tauri_plugin_notification`, `tauri_plugin_store`.
- Permissions: `src-tauri/capabilities/default.json` — `core:default`, `opener:default`, `deep-link:default`, `notification:default`, `store:default`, `dialog:default`, and `http:default` with an allow-list of `http://*` and `https://*`. New plugins require a permission entry here or their IPC is blocked.
- Frontend builds to `dist/`; Tauri reads it from `../dist` (`tauri.conf.json` `frontendDist`).

## Conventions & gotchas

- Vite port `1420` is `strictPort: true` — do not change it; Tauri's `devUrl` expects it. Vite is configured to ignore `src-tauri/**`.
- Tailwind v4: no `tailwind.config.js`. Theme is defined via `@theme` in `src/index.css`; shadcn design tokens live there as CSS vars (oklch, light + `.dark`).
- shadcn components live in `src/components/ui/`; add with `pnpm dlx shadcn@latest add <name>`. Aliases and style are in `components.json`.
- `cn()` merge helper is in `src/lib/utils.ts` (clsx + tailwind-merge). Shared non-UI helpers live in `src/lib/tools.ts`, `src/lib/auth.ts`, `src/lib/query-client.ts`, `src/lib/deep-link.ts`, and `src/lib/hooks/`.
- `src-tauri/src/main.rs` sets `windows_subsystem = "windows"` in release — **do not remove** (prevents an extra console window on Windows).
- `.gitignore` excludes `.codegraph/` and `.docs/`; no CI workflows are configured.
