NEXT_PUBLIC_INSFORGE_URL="https://your-project.us-east.insforge.app"
NEXT_PUBLIC_INSFORGE_ANON_KEY=""
# Public origin used for copyable webhook URLs ({APP_URL}/api/hooks/{token})
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Server-only InsForge project admin API key (webhook receiver writes). Never use NEXT_PUBLIC_*.
INSFORGE_API_KEY=""

# Groq API Key (https://console.groq.com/keys)
GROQ_API_KEY=""
